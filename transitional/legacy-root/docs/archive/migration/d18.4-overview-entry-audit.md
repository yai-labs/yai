# D18.4 Overview Entry Audit

## Entry Surface Today
- `docs/README.md`: valid structural contract, but oriented to section inventory rather than platform mental model.
- `docs/architecture/README.md`: strong section spine, but entry emphasis still tied to documentation mechanics.
- `docs/architecture/overview/README.md`: thin index that elevates refoundation artifacts (`canonical-tree-target`, `repository-scope`) as primary entrypoints.

## Competing/Weak Entry Materials
- `docs/architecture/overview/`
  - classification: refactor-derived entry material
  - action: declass to support archive/migration pointer
- `docs/architecture/overview/system-overview.md`
  - classification: supporting entry material
  - action: keep live as implementation-boundary support, not first entry

## Canonical Entry Candidates
- New system-level overview document (platform narrative first).
- New subsystem map document (domain map first).
- New repository map document (implementation map first).
- New docs navigation map (reader journey first).

## Required Rebuild Outcome
- Root entry explains YAI platform, not docs cleanup history.
- Architecture entry starts from system model and subsystem topology.
- Overview section has explicit canonical reading order and supporting legacy note separation.
