# Datasets

## Canonical Data Surface
- `data/datasets/`
- Current dataset baseline: `data/datasets/global-stress/v1/`

## Rules
- Canonical datasets live only under `data/datasets/`.
- Documentation describes usage and format; it is not a data source.
- Runtime artifacts and logs remain outside `data/datasets/`.
