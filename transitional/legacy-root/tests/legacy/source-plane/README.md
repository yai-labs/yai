# Legacy Source Plane Tests

This suite preserves workspace-era source-plane integration flows as legacy regression material.

Canonical replacement target:
- `tests/integration/source-plane/*` with container/system-centered semantics.
