# Architecture

Technical architecture by domain/layer.
This content must remain consistent with `core/` and `boundaries/`.

- `kernel/`
- `engine/`
- `api/`
- `runtime/`
- `legacy/` (historical docs)
