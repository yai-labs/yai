{
  "required_fields": [
    "timestamp",
    "ws_id",
    "trace_id",
    "event.type",
    "event.source",
    "principal.id",
    "principal.role",
    "target.pipeline_id",
    "target.run_id",
    "target.params_hash",
    "target.dataset_ref",
    "decision.outcome",
    "decision.reason_code",
    "decision.baseline_hash",
    "enforcement.result",
    "metrics.time_to_decide_ms",
    "metrics.run_started"
  ],
  "notes": {
    "params_hash": "A stable hash of declared parameters is mandatory for reproducibility.",
    "dataset_ref": "Should be a pinned reference (version/hash), not a floating tag.",
    "run_started": "Must be false when decision is deny/quarantine for this pack's vectors."
  }
}
