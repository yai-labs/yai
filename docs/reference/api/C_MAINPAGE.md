# YAI API Reference (Runtime)

This reference is generated from the public headers in `boot/`, `core/`, `kernel/`, `engine/`, and `runtime/`.

Canonical contracts live in `deps/yai-specs/`.
If runtime code disagrees with specs, the code is wrong.
