# Protocol Reference

Lookup-oriented protocol reference documentation.

- Contract and binding details live under `contracts/`.
- Historical protocol migration notes must live under `docs/archive/`.
