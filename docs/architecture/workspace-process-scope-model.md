# Workspace Process Scope Model

Process scope is represented in envelope metadata as a first-class scope flag.
Current runtime does not yet create per-workspace process namespaces; it exposes process-scope readiness hooks for future enforcement backends.
