# Workspace Privilege Scope Model

Privilege scope is explicitly distinct from normative authority.
- Authority: decision/control semantics from law stack.
- Privilege scope: OS/runtime privilege isolation semantics.

Current runtime models privilege scope metadata but does not yet enforce OS-level privilege separation.
