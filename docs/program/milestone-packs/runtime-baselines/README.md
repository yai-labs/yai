# Runtime Baselines (Program)

Program baseline packs and execution baselines consolidated from legacy runbook-program paths.

This section is program-facing and milestone-oriented.

- Live operational procedures belong under `docs/runbooks/operations/`.
- Historical/superseded baseline material belongs under `docs/archive/`.
- Architecture source-of-truth remains under `docs/architecture/`.
