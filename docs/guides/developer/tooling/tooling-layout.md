# Tooling Layout

High-level layout:
- `tools/bin/` executable wrappers
- Governance/tooling implementation: canonical in `infra/tools/`
- `infra/tools/schemas/` schema contracts

Docs should reference tooling by stable entrypoints under `tools/bin/`.
